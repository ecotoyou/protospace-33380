<main class="main">
  <div class="inner">
      <div class="greeting">
      <% if user_signed_in? %>
        <div> こんにちは<%= link_to current_user.name, user_path(current_user.id) %>さん </div>
      <% end %>
    <div class="card__wrapper">
      <%= render partial: 'prototype', collection: @prototypes %>
    </div>
  </div>
</main>
